// Save it using the Chrome extension storage API.
/* chrome.storage.local.set({foo: 'hello', jwtToken: localStorage['token']}, function() {
    console.log('Settings saved');
}); */

// putting code in React instead //